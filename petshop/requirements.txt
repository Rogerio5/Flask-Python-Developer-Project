# Framework principal
Flask==3.0.3

# Banco de dados e ORM
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36

# Autenticação e sessão
Flask-Login==0.6.3
Werkzeug==3.0.4

# Migrações de banco (muito útil para evoluir o schema)
Flask-Migrate==4.0.7
alembic==1.14.0

# Testes
pytest==8.3.3
pytest-flask==1.3.0

# Utilitários
python-dotenv==1.0.1   # para variáveis de ambiente (.env)
email-validator==2.2.0 # se quiser validar emails em formulários
